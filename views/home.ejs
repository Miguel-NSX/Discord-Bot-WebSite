
<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login com Discord</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            min-height: 100vh;
            background: linear-gradient(45deg, #ff69b4, #1a1a1a);
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            color: white;
        }

        h1 {
            font-size: 3.5em;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }

        .tagline {
            font-size: 1.5em;
            margin-bottom: 30px;
            text-align: center;
            text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.3);
        }

        .discord-btn {
            background-color: #7289da;
            color: white;
            padding: 15px 30px;
            border-radius: 5px;
            text-decoration: none;
            font-size: 1.2em;
            transition: transform 0.2s, background-color 0.2s;
            border: none;
        }

        .discord-btn:hover {
            background-color: #5f73bc;
            transform: scale(1.05);
        }

        .user-info {
            text-align: center;
            margin-top: 20px;
        }

        .nav-links {
            margin-top: 20px;
            display: flex;
            gap: 15px;
        }

        .nav-links a {
            color: white;
            text-decoration: none;
            padding: 10px 20px;
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 5px;
            transition: background-color 0.2s;
        }

        .nav-links a:hover {
            background-color: rgba(255, 255, 255, 0.2);
        }
    </style>
    <link rel="stylesheet" href="/css/menu.css">
</head>
<body>
    <nav class="navbar">
        <a href="/" class="navbar-brand">Jully</a>
        <button class="hamburger" onclick="toggleMenu()">
            <span></span>
            <span></span>
            <span></span>
        </button>
        <ul class="nav-items">
            <li><a href="/">Início</a></li>
            <li><a href="/sobre">Sobre</a></li>
            <li><a href="/contato">Contato</a></li>
        </ul>
    </nav>

    <script>
        function toggleMenu() {
            document.querySelector('.hamburger').classList.toggle('active');
            document.querySelector('.nav-items').classList.toggle('active');
        }
    </script>
    <h1>Jully</h1>
    <p class="tagline">A fazenda dos seus sonhos começa aqui!</p>
    <% if (user) { %>
        <div class="user-info">
            <p>Olá, <%= user.username %>!</p>
            <div class="nav-links">
                <a href="/dashboard">Ir para Dashboard</a>
                <a href="/auth/logout">Sair</a>
            </div>
        </div>
    <% } else { %>
        <a href="/auth/discord" class="discord-btn">Login com Discord</a>
    <% } %>
</body>
</html>
